// 백준 2667 - 단지번호붙이기 (Silver 1)  [실전 모드: 백지에서 직접]
// https://www.acmicpc.net/problem/2667
//
// 연결 요소(Connected Component)의 정석. 1012를 풀었다면 그 응용.
// 차이점: 단지(그룹) 개수 + "각 단지의 집 수"를 오름차순 정렬해 출력.
//   1) 격자에서 연결된 1들을 DFS/BFS로 묶으며 그 크기(집 수)를 센다.
//   2) 단지 수 출력 → 각 단지 크기를 정렬해 한 줄씩 출력.
//
// 막히면 "힌트" / 정답은 "정답 보여줘".
// 레퍼런스(웹 검증, DFS+BFS 둘 다): https://ansohxxn.github.io/boj/2667/

#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);

    // 백지. 설계 → 구현.

    return 0;
}
